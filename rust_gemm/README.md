# rust_gemm
## Matrix Multiplication Benchmark (CPU vs GPU)  

### Build and Run it (Generic) :hammer_and_pick:

  > * __Run it Locally__:
    
    ```bash
          cargo run --release
     ```  

  > * __Build and Run it Docker__:
    
    ```bash
          docker build -t rust_gemm .
          docker run -it rust_gemm
     ```  


